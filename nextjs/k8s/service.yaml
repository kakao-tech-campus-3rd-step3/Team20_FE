apiVersion: v1
kind: Service
metadata:
  name: kspot-nextjs-service
  labels:
    app: kspot-nextjs
    environment: production
spec:
  type: LoadBalancer  
  selector:
    app: kspot-nextjs
  ports:
  - protocol: TCP
    port: 80       
    targetPort: 3000  
  sessionAffinity: ClientIP 
